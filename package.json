{
  "name": "openclaw-lovelogic",
  "version": "0.1.0",
  "description": "LoveLogicAI downstream layer for OpenClaw — consent-native memory kernel, deployment config, and agent skills.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
